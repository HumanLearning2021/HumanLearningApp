<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.HumanLearning2021.HumanLearningApp">

    <uses-permission
        android:name="android.permission.CAMERA"
        android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HumanLearning">
        <activity android:name=".view.DisplayImageSetActivity"/>
        <activity android:name=".DataOverviewActivity"/>
        <activity android:name=".DataCreationActivity" />
        <activity android:name=".AddPictureActivity" />
        <activity android:name=".view.DisplayImageActivity" />
        <activity android:name=".view.DisplayDatasetActivity" />
        <activity android:name=".view.LearningActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>